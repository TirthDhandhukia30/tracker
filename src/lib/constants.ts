/**
 * Application-wide constants
 */

/** The earliest date the app should allow data entry/viewing */
export const START_DATE = new Date('2025-12-31');

/** Debounce delay for auto-saving entries (ms) */
export const SAVE_DEBOUNCE_MS = 800;

/** Number of days to show in weight history chart */
export const WEIGHT_HISTORY_DAYS = 30;

/** Number of data points to display in the sparkline chart */
export const CHART_DATA_POINTS = 14;
